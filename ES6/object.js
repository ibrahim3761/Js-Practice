const person = {
    name:"hena",
    age:24,
    friends:['karim', 'hero alam'],
    details:{
        job: 'yes',
        isMArried: true,
        status: "not found",
        father:{
            name:'asbd'
        }
    }
}

// console.log(person["details"]["job"]);
